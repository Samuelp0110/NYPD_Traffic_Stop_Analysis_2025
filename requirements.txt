# --- Core data analysis ---
pandas>=2.2.0
numpy>=1.26.0

# --- PostgreSQL connectivity ---
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.30

# --- Jupyter environment ---
jupyterlab>=4.1.0
ipykernel>=6.29.0

# --- Optional: performance / helpers (safe to remove if not needed) ---
tqdm>=4.66.0          # progress bars for long transformations
python-dotenv>=1.0.1  # load DB credentials from .env
pyarrow>=16.0.0       # improves I/O performance for large CSVs